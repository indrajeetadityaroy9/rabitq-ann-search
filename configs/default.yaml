index:
  bits: 1
  seed: 42

search:
  k: 10
  recall_targets: [0.80, 0.90, 0.95, 0.97, 0.99]

data:
  base_dir: data/
  dataset: sift1m

eval:
  num_threads: 0  # 0 = auto-detect
  warmup_queries: 100
