index:
  M: 32
  ef_construction: 200
  seed: 42

search:
  k: 10
  ef_values: [10, 20, 40, 80, 100, 200, 400]

data:
  base_dir: data/
  dataset: sift1m

eval:
  num_threads: 0  # 0 = auto-detect
  warmup_queries: 100
