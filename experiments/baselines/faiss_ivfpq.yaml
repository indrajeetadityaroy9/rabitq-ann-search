# Faiss IVF-PQ baseline configuration.
# Used by a separate baseline runner (not CP-HNSW).

method: faiss_ivfpq
params:
  nlist: 1024
  m_subquantizers: 8
  nbits: 8
  nprobe_values: [1, 4, 8, 16, 32, 64, 128]

search:
  k: 10

data:
  base_dir: data/
  dataset: sift1m
