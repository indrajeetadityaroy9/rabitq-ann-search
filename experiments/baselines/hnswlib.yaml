# hnswlib baseline configuration.
# Used by a separate baseline runner (not CP-HNSW).

method: hnswlib
params:
  M: 32
  ef_construction: 200

search:
  k: 10
  ef_values: [10, 20, 40, 80, 100, 200, 400]

data:
  base_dir: data/
  dataset: sift1m
